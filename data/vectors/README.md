# Vector store index and metadata are auto-generated here.
# Files created on first run:
#   index.pkl          — FAISS-lite float vectors (pickle)
#   metadata.json      — Full document metadata
#   content_hashes.json — MD5 deduplication map
